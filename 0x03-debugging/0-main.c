#include "main.h"

/**
 * main - tests of function that prints if integer is positive or negetive
 *
 * Return: Always (0)
 */
int main(void)
{
	int i;

	i = 0;
	positive_or_negative(i);

	return (0);
}
