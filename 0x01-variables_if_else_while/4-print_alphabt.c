#include <stdio.h>

/**
 * main - Program that prints alphabet in lowercase
 *
 * Return: Always (success message)
 */
int main(void)
{
	char c;

	for (c = 'a'; c <= 'z'; c++)
	{
		if (c != 'e' && c != 'q')
		{
			putchar(c);
		}
	}
	putchar('\n');
	return (0);
}
